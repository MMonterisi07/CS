'''
Name: Maddox Monterisi
Date: 5/23/25
Description: A password keeper that can store passwords, lets you input more, change them, and more
Bugs: none
Challenges: Allow the user to retroactively add more usernames and passwords
Allow the user to change usernames and passwords
Store the list of websites with usernames and passwords in an excel spreadsheet
Require a password to enter the password keeper
Sources: none
'''
import csv
while True:
    password = input("Enter a password to get into the code: ")
    if password == "pass":
        break
    else:
        print("Wrong. Try again.")
def user_entry(websites, usernames, passwords):
    '''
    User adds an entry to the parallel array of websites, usernames, and passwords
    Args:
        websites (list): the list of websites
        usernames (list): the list of usernames
        passwords (list): the list of passwords
    Returns:
        parallel array: newly added to array of websites, usernames, and passwords
    '''
    website = input("Enter a website: ")
    username = input("Enter a username: ")
    password = input("Enter your password: ")
    websites.append(website)
    usernames.append(username)
    passwords.append(password)
    with open("Passwords_and_Usernames_and_Websites.csv", "w", newline='') as csvfile:
        writer = csv.writer(csvfile)
        writer.writerow(passwords)
        writer.writerow(usernames)
        writer.writerow(websites)
    return websites, usernames, passwords
def print_entry(website, username, password):
    '''
    takes three elements (a website, username, and password trio) and prints them neatly in an f-string.
    Args:
	    Website
	    Username
	    Password
    Returns: 
	    Print (f-string)
    '''
    print(f"The website is {website}, the username is {username} and the password is {password}")
def get_index(websites):
    '''
    takes the list of websites. This function should prompt the user for a website and return the index of that website in the given list of websites.
	Args:
		websites
	Return:
		Index of website in websites
	'''
    while True:
        user_website = input("Enter a website: ")
        if user_website in websites:
            return websites.index(user_website)
        else:
            print("Website not here.")
def change_info(websites, usernames, passwords):
    '''
    takes three elements (a website, username, and password trio) and prints them neatly in an f-string.
    Args:
	    Website 
	    Username
	    Password
    Returns: 
	    the edited username or password
    '''
    index = get_index(websites)
    usernames[index] = input("What is your new username? ")
    passwords[index] = input("What is your new password? ")
def main():
    websites = []
    usernames = []
    passwords = []
    user_entry(websites, usernames, passwords)
    while True:
        option = input('''
Which option would you like to do? Press q to quit.
1. add a website, username, and password.
2. print every inputed website, username, and password
3. print a specific website, username, and password based on its index.
4. change a username or password
Enter here: ''')
        if option == "q":
            break
        elif option == "1":
            user_entry(websites, usernames, passwords)
        elif option == "2":
            for index in range(len(websites)):
                print_entry(websites[index], usernames[index], passwords[index])
        elif option == "3":
            index = get_index(websites)
            print_entry(websites[index], usernames[index], passwords[index])
        elif option == "4":
            change_info(websites, usernames, passwords)
main()